platform: linux
inputs:
    name: sourcecode
    name: maven-docker-image

outputs:
    name: project-package
    
run:
    path: mvn 
    args: ["-f", "sourcecode/pom.xml", "-DskipTests=true","-DoutputDirectory=project-package","package"]

